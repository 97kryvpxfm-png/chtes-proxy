# Chutes AI Image Proxy для Termux

Прокси-сервер на Python для генерации изображений через Chutes AI с кэшированием результатов. Разработан специально для запуска в Termux на Android.

## Возможности
- 🚀 **Простой запуск**: Интерактивное меню настройки.
- 💾 **Кэширование**: Повторные запросы с теми же параметрами возвращаются мгновенно из локального кэша.
- 🔧 **Гибкость**: Поддержка негативных промптов, настройки размеров изображения и выбора модели.
- 📱 **Android-Ready**: Оптимизировано для работы в Termux.

## Установка в Termux

1. **Обновите пакеты и установите Python:**
   ```bash
   pkg update && pkg upgrade
   pkg install python
   ```

2. **Скопируйте файлы проекта в папку на устройстве.**

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите приложение:**
   ```bash
   python server.py
   ```

## Настройка и первый запуск

1. Запустите скрипт командой `python server.py`.
2. В меню выберите пункт **1** и введите ваш API ключ Chutes AI.
3. В меню выберите пункт **2** и введите **имя модели** (например, `Illustrij` или `z-image-turbo`).
   - Имя модели можно найти на странице публичного chute на сайте Chutes AI (кнопка "Copy model name").
   - **Важно:** Система спросит, является ли провайдер "Chutes". Если возле имени модели указан провайдер **chutes**, ответьте `y`. Для остальных (например, Illustrij) ответьте `n`.
4. Выберите пункт **4**, чтобы запустить сервер.

Сервер будет доступен по адресу: `http://localhost:4444`

## Использование API

Формат URL:
`http://localhost:4444/prompt/<PROMPT>/<NEGATIVE_PROMPT>/<WIDTH>x<HEIGHT>`

### Примеры запросов

**1. Простой запрос (размер по умолчанию 1024x1024):**
```
http://localhost:4444/prompt/beautiful%20sunset
```

**2. С негативным промптом:**
```
http://localhost:4444/prompt/beautiful%20sunset/blurry,%20low%20quality
```

**3. С указанием размеров:**
```
http://localhost:4444/prompt/cyberpunk%20city/low%20quality/512x768
```

**4. Только размеры (без негативного промпта):**
Используйте `-` или пробел вместо негативного промпта.
```
http://localhost:4444/prompt/cute%20cat/-/1280x720
```

## Структура проекта
- `server.py` — основной файл приложения.
- `config.json` — файл конфигурации (создается автоматически).
- `cache/` — папка с кэшированными изображениями (создается автоматически).
- `requirements.txt` — список зависимостей.

## Решение проблем

- **Ошибка 401**: Проверьте правильность API ключа в настройках.
- **Ошибка 404**: Проверьте, что имя модели указано правильно.
- **Ошибка 429**: Закончился лимит бесплатных запросов на сегодня.
