# Chutes AI Image Proxy для Termux

Прокси-сервер на Python для генерации изображений через Chutes AI с кэшированием результатов. Разработан специально для запуска в Termux на Android.

## Возможности
- 🚀 **Простой запуск**: Интерактивное меню настройки.
- 🧠 **Умное обучение**: Скрипт знает популярные модели, а новым может "обучаться" через пример curl-запроса.
- 💾 **Кэширование**: Повторные запросы возвращаются мгновенно из локального кэша.
- 🔗 **Настройка ссылки**: Вы сами выбираете, включать ли в ссылку параметры (негатив, разрешение) в зависимости от возможностей модели.
- 📱 **Android-Ready**: Оптимизировано для работы в Termux.

## Установка в Termux

1. **Обновите пакеты и установите Python:**
   ```bash
   pkg update && pkg upgrade
   pkg install python
   ```

2. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/97kryvpxfm-png/chtes-proxy
   cd chtes-proxy
  

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите приложение:**
   ```bash
   python server.py
   ```

## Настройка и первый запуск

1. Запустите скрипт командой `python server.py`.
2. В меню выберите пункт **1** и введите ваш API ключ Chutes AI.
3. В меню выберите пункт **2** и введите **имя модели** (например, `Illustrij`, `FLUX.1-schnell`, `z-image-turbo`).
   - Если модель известна скрипту, она сразу применится.
   - Если модель новая, скрипт попросит вставить пример **CURL-запроса** (его можно скопировать на сайте Chutes). Скрипт проанализирует запрос и запомнит возможности модели.
4. (Опционально) Выберите пункт **3**, чтобы настроить формат ссылки (включать ли негативный промпт и разрешение).
5. Выберите пункт **5**, чтобы запустить сервер.

Сервер будет доступен по адресу: `http://localhost:4444`

## Использование API

Формат URL зависит от ваших настроек (пункт 3 в меню).
Пример полной ссылки:
`http://localhost:4444/prompt/[PROMPT]/[NEGATIVE_PROMPT]/[WIDTH]x[HEIGHT]`

### Примеры запросов

**1. Простой запрос (если в настройках ссылки только PROMPT):**
```
http://localhost:4444/prompt/beautiful%20sunset
```

**2. С параметрами (если включены в настройках):**
```
http://localhost:4444/prompt/beautiful%20sunset/blurry,%20low%20quality/512x768
```

**Важно:** В браузере пробелы работают автоматически. Если используете ссылку в коде или CSS, заменяйте пробелы на `%20`.

## Структура проекта
- `server.py` — основной файл приложения.
- `config.json` — файл конфигурации (создается автоматически, хранит обученные модели).
- `cache/` — папка с кэшированными изображениями (создается автоматически).
- `requirements.txt` — список зависимостей.

## Решение проблем

- **Ошибка 400 (Bad Request)**: Возможно, вы пытаетесь отправить параметры (размер, негатив) модели, которая их не поддерживает (например, Native Chutes). Проверьте настройки ссылки (пункт 3).
- **Ошибка 401**: Проверьте правильность API ключа в настройках.
- **Ошибка 404**: Проверьте имя модели.

## Как обновить
Если вышла новая версия, просто выполните команду в папке проекта:
```bash
git pull
```
